/**
 * HTML - JS
 */

console.log("Hello HTML  - JS linkado")


function hello(){
    let nome
    nome = prompt("Qual é o seu nome?") // prompt só funciona no navegador não roda no node, abre a caixa de mensagem
    alert (`Hello ${nome}`)
}
